
// Challenge area

// name, age, location

//Christine is 27 and lives in Oklahoma City 
// increase age by 1 and print the

let myPerson = {
    name: 'Christine',
    age: 27,
    city: 'Oklahoma City'
}

console.log(`${myPerson.name} is ${myPerson.age} and lives in ${myPerson.city}.`)

let myPersonC = {
    name: 'Christine',
    age: 27 + 1,
    city: 'Oklahoma City'
}

console.log(`${myPersonC.name} is ${myPersonC.age} and lives in ${myPersonC.city}.`)